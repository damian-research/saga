// import type { Bookmark } from "../models/bookmarks.types";

// export async function addBookmarkMock(bookmark: Bookmark): Promise<Bookmark> {
//   // simulate network / storage delay
//   await new Promise((r) => setTimeout(r, 300));

//   // echo back the same record (mock persistence)
//   console.log("Bookmark saved:", bookmark);
//   return bookmark;
// }
